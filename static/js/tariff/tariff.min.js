/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
/******/ (function() { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./src/js/pages/tariff.js":
/*!********************************!*\
  !*** ./src/js/pages/tariff.js ***!
  \********************************/
/***/ (function() {

eval("document.addEventListener('DOMContentLoaded', function () {\n  'use strict';\n\n  switchDuration();\n  planSelection();\n  qrCheckboxToggle();\n  promo();\n});\nvar switchDuration = function switchDuration() {\n  var switcher = document.querySelector('.switch'),\n    plans = document.querySelectorAll('.tariff-plan'),\n    planPricePlace = document.querySelector('.tariff-order-col.plan-price span');\n  if (!switcher || !plans.length || !planPricePlace) return;\n  switcher.addEventListener('click', function () {\n    if (switcher.classList.contains('active')) switcher.classList.remove('active');else switcher.classList.add('active');\n    plans.forEach(function (plan) {\n      var singleYearPrice = plan.dataset.price,\n        tenYearsPrice = plan.dataset.priceTen,\n        amountEl = plan.querySelector('.tariff-plan-price-amount');\n      if (!singleYearPrice || !tenYearsPrice || !amountEl) return;\n      if (switcher.classList.contains('active')) amountEl.innerHTML = tenYearsPrice;else amountEl.innerHTML = singleYearPrice;\n    });\n    var activePlan = document.querySelector('.tariff-plan.active');\n\n    // If there's an active plan - get its price and set to totals table.\n    if (activePlan) {\n      if (switcher.classList.contains('active')) planPricePlace.innerHTML = activePlan.dataset.priceTen;else planPricePlace.innerHTML = activePlan.dataset.price;\n    }\n    updateTotal();\n  });\n};\nvar planSelection = function planSelection() {\n  var plans = document.querySelectorAll('.tariff-plan'),\n    qrMetalPriceEl = document.querySelector('.tariff-order-rust-qr-price');\n  if (!plans.length) return;\n  plans.forEach(function (plan) {\n    plan.addEventListener('click', function () {\n      var activePlan = document.querySelector('.tariff-plan.active'),\n        qrMetalPrice = plan.dataset.qrMetalPrice || '',\n        planName = plan.querySelector('.tariff-plan-title').innerHTML,\n        planPrice = plan.querySelector('.tariff-plan-price-amount').innerHTML,\n        planNamePlace = document.querySelector('.tariff-order-col.plan-name'),\n        planPricePlace = document.querySelector('.tariff-order-col.plan-price span'),\n        orderPart = document.querySelector('.tariff-right');\n      if (activePlan) activePlan.classList.remove('active');\n      plan.classList.add('active');\n      qrMetalPriceEl.innerHTML = qrMetalPrice;\n      if (orderPart) orderPart.classList.remove('hidden');\n      if (planName && planNamePlace) planNamePlace.innerHTML = planName;\n      if (planPrice && planPricePlace) planPricePlace.innerHTML = planPrice;\n      updateTotal();\n    });\n  });\n};\nvar qrCheckboxToggle = function qrCheckboxToggle() {\n  var checkbox = document.querySelector('.tariff-order-rust-qr'),\n    qrPricePlace = document.querySelector('.tariff-order-col.qr-price span');\n  if (!checkbox || !qrPricePlace) return;\n  checkbox.addEventListener('click', function () {\n    var activePlan = document.querySelector('.tariff-plan.active');\n    if (!activePlan) return;\n    if (checkbox.classList.contains('active')) {\n      checkbox.classList.remove('active');\n      qrPricePlace.innerHTML = 0;\n    } else {\n      checkbox.classList.add('active');\n      qrPricePlace.innerHTML = activePlan.dataset.qrMetalPrice;\n    }\n    updateTotal();\n  });\n};\n\n/**\r\n * Calculate total price.\r\n *\r\n * @returns {number}\tTotal price.\r\n */\nvar calculateTotal = function calculateTotal() {\n  var plan = document.querySelector('.tariff-plan.active'),\n    qrCheckbox = document.querySelector('.tariff-order-rust-qr.active'),\n    tenYears = document.querySelector('.tariff-duration .switch.active');\n  var qrPrice = qrCheckbox && plan ? parseFloat(plan.dataset.qrMetalPrice.replace(' ', '')) : 0,\n    planPrice;\n  if (!plan) return 0;\n\n  // Check if 10 years switch is ON.\n  if (tenYears) planPrice = parseFloat(plan.dataset.priceTen.replace(' ', '')) * 10;else planPrice = parseFloat(plan.dataset.price.replace(' ', ''));\n  return planPrice + qrPrice;\n};\n\n/**\r\n * Set total price in HTML.\r\n */\nvar updateTotal = function updateTotal() {\n  var totalPlace = document.querySelector('.tariff-order-col.total-price span');\n  if (!totalPlace) return;\n  totalPlace.innerHTML = calculateTotal();\n};\n\n/**\r\n * Promo code form.\r\n */\nvar promo = function promo() {\n  var form = document.querySelector('.tariff-order-promo'),\n    input = document.querySelector('#promo');\n  if (!form || !input) return;\n  var btn = form.querySelector('.btn[type=\"submit\"]');\n  input.addEventListener('keyup', function (e) {\n    var val = e.target.value;\n    if (btn && val) btn.removeAttribute('disabled');else btn.setAttribute('disabled', 'disabled');\n  });\n};\n\n//# sourceURL=webpack://inheart/./src/js/pages/tariff.js?");

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = {};
/******/ 	__webpack_modules__["./src/js/pages/tariff.js"]();
/******/ 	
/******/ })()
;